{
  "name": "emoji-styles-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter demo dev",
    "generate": "pnpm --filter emoji-styles-scripts generate"
  }
}
